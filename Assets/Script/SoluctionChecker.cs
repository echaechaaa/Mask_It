using System.Collections.Generic;
using UnityEngine;

public class SoluctionChecker : MonoBehaviour
{
    /*[EasyButtons.Button]
    public bool IslevelSuccess(LevelData _currentlevel, List<Card> masked, List<Card> displayed)
    {
        if (masked[0].PrefabSource == _currentlevel.solutionMasks[0].CardUI.Card)
        {
            Debug.Log("card is same in mask");
            if (masked[0].CardUI.currentRot == _currentlevel.solutionMasks[0].allowedRotation[0])
            {
                Debug.Log("card rot is same in mask");

            }
        }
        return false;
    }*/
}
